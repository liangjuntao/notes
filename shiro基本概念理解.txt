realm:
	一个访问用户角色，权限的组件。
	一个DAO，数据访问对象。
	realm接口的实现使用特定数据源（如jdbc，其实也就是配置文件中的datasource）来发现授权数据（角色，权限等）
	其实就是在realm中通过自己定义的UserDao去数据库拿角色，权限，然年把这些数据交给shiro，这也是为啥说realm可以将特定格式的数据转化未shiro理解的数据格式。

subject：
	一个理想的安全术语。官网的翻译好蛋疼啊。
	对应要做某件事情的主体的一个表示。一个访问应用的进程，一个要登陆的用户！

	
shiro的session什么时候写入